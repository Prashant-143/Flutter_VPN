import 'package:flutter/material.dart';

class NetworkData {
  String title, subtitle;
  Icon icon; // Change the type of icon back to Widget

  NetworkData(
      {required this.title, required this.icon, required this.subtitle});
}
